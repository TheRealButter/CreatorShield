# CreatorShield

CreatorShield is a lightweight CLI that applies a simple "shield" transformation to messages.

## Usage

```bash
python -m creatorshield --message "Hello"
```

## Development

```bash
python -m pip install -e .
pytest
```
